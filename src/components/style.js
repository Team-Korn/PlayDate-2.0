import styled from 'styled-components';

const StyledButton = styled.button`
  padding: 10px 20px;
  text-align: center;
  cursor: pointer;
  border-radius: 16px;
`;

export default {
  StyledButton,
};
